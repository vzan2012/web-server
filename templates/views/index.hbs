<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <title>{{title}}</title>

    {{!-- Styles --}}
    {{>styles}}

</head>

<body>
    {{>header}}

    <div class="container pg-contents mt-5">
        <div class="row-fluid">
            <h1>{{title}}</h1>
            {{!-- <p><strong>Beginner User:</strong> {{name}}</p>
            <a href="/about">Click here - About Page</a> --}}

            <strong>Use this website to search the weather</strong>
            <form class="form-inline mt-3">
                <div class="form-group mr-1">
                    <input type="text" class="form-control" name="" placeholder="Enter the Location" id="txtLocation" />
                </div>
                <div class="form-group">
                    <button id="btnSubmit" class="btn btn-primary">Submit</button>
                </div>
            </form>

            <div class="card mt-3 mb-5 text-white msg-success bg-primary">
                <div class="card-body"></div>
            </div>

            <div class="card mt-3 mb-5 text-white msg-error bg-danger">
                <div class="card-body"></div>
            </div>

        </div>
    </div>

    {{>footer}}


    {{!-- Scripts  --}}
    {{>scripts}}
</body>

</html>